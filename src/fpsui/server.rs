use ambient_api::prelude::*;

#[main]
pub fn main() {
    // messages::StartGame::subscribe(|_, msg| {
    //     messages::StartGame {
    //         name: msg.name.clone(),
    //         id: msg.id,
    //         uid: msg.uid.clone(),
    //     }
    //     .send_local_broadcast(false);
    //     // println!("{:?}", msg.name);
    // });
}
