[components]

speed = { type = "F32", name = "speed", description = "speed", attributes = [
    "Debuggable",
    "Networked",
] }

animation_blend_list = { type = { type = "Vec", element_type = "EntityId" }, name = "anim_list", description = "anim_list", attributes = [
    "Debuggable",
    "Networked",
] }


animation_player_list = { type = { type = "Vec", element_type = "EntityId" }, name = "anim_list", description = "anim_list", attributes = [
    "Debuggable",
    "Networked",
] }


movement = { type = "Vec2", name = "movement", description = "movement", attributes = [
    "Debuggable",
    "Networked",
] }


offground = { type = "Bool", name = "jump", description = "jump", attributes = [
    "Debuggable",
    "Networked",
] }


running = { type = "Bool", name = "speed", description = "speed", attributes = [
    "Debuggable",
    "Networked",
] }
