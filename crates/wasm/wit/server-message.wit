default interface server-message {
    use pkg.types.{entity-id}

    variant source {
        network(string),
        module(entity-id),
    }

    variant target {
        player-broadcast-unreliable,
        player-broadcast-reliable,
        player-targeted-unreliable(string),
        player-targeted-reliable(string),
        module-broadcast,
        module(entity-id),
    }

    send: func(target-id: target, name: string, data: list<u8>)
}