[components."core::wasm::module"]
name = "Module"
description = "A module."
type = "Empty"
attributes = ["Networked", "Store", "Debuggable"]

[components."core::wasm::module_on_server"]
name = "Module on server"
description = "Whether or not this module is on the server."
type = "Empty"
attributes = ["Networked", "Store", "Debuggable"]

[components."core::wasm::module_enabled"]
name = "Module enabled"
description = "Whether or not this module is enabled."
type = "Bool"
attributes = ["Networked", "Store", "Debuggable"]

[components."core::wasm::module_name"]
name = "Module name"
description = "The name of this module."
type = "String"
attributes = ["Networked", "Store", "Debuggable"]