[package]
name = "ambient_package_json"
edition = "2021"
description = "The structure of the ambient_package.json file generated by the build system."
license = "MIT OR Apache-2.0"
repository = "https://github.com/AmbientRun/Ambient"
rust-version = { workspace = true }
version = { workspace = true }

[package.metadata.docs.rs]
rustdoc-args = ["--cfg", "docsrs"]
targets = ["wasm32-wasi"]

[dependencies]
serde = { workspace = true }
indexmap = { workspace = true }
pathdiff = { workspace = true }

ambient_primitive_component_definitions = { path = "../primitive_component_definitions" , version = "0.3.2-nightly-2024-01-09" }
