[package]
id = "ambient_example_unity"
name = "Unity"
description = "Load and spawn a Unity prefab"
version = "0.0.1"
content = { type = "Playable", example = true }

[dependencies]
orbit_camera = { path = "../../../packages/std/orbit_camera" }
