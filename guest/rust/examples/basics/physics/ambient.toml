[ember]
id = "physics"
organization = "ambient"
name = "Physics"
version = "0.0.1"

[messages.bonk]
description = "Collision between two objects."
fields = { emitter = "entity-id", listener = "entity-id" }