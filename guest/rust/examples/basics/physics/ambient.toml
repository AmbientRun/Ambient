[ember]
id = "ambient_example_physics"
name = "Physics"
version = "0.0.1"
repository = "https://github.com/AmbientRun/Ambient/tree/main/guest/rust/examples/basics/physics"
type = "Game"
categories = ["Example"]

[messages.Bonk]
description = "Collision between two objects."
fields = { emitter = "EntityId", listener = "EntityId" }
