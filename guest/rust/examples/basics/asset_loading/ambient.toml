[ember]
id = "ambient_example_asset_loading"
name = "Asset Loading"
description = "This shows how to load assets, and how to work with the asset pipeline."
version = "0.0.1"
repository = "https://github.com/AmbientRun/Ambient/tree/main/guest/rust/examples/basics/asset_loading"
type = "Game"
categories = ["Example"]

[components.is_the_best]
type = "bool"
name = "Is The Best"
description = "Declares whether or not the entity this is attached is the best."
attributes = ["networked", "debuggable"]
