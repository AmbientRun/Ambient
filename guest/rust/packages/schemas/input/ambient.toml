[package]
id = "input_schema"
name = "Input Schema"
description = "Schema for input interoperability"
version = "0.0.1"
content = { type = "Asset", schema = true }

[messages.RequestInput]
fields = {}

[messages.ReleaseInput]
fields = {}
